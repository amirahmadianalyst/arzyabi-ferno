# گزارش تحلیل فایل‌های Excel — سامانه ارزیابی عملکرد پرسنل فرنو

این گزارش نتیجه بررسی مستقیم و برنامه‌نویسی‌شده (با Python/openpyxl) تمام ۷ فایل Excel آپلودشده
است. هیچ مقداری در این گزارش حدس زده نشده؛ همه از محتوای واقعی Sheetها استخراج شده‌اند.

## ۱. فایل‌ها

| فایل | کاربرد | مسئول مربوطه | Sheetها |
|---|---|---|---|
| `نمره دهی مسئول.xlsx` | ارزیابی هفتگی توسط مسئول هر بخش | مسئول بخش | مترجم، مرجع، سالن، کال‌سنتر، صندوق، آشپزخانه، تولید |
| `نمره دهی بازرس.xlsx` | ارزیابی توسط بازرس کیفیت | بازرس | مترجم، مرجع، آشپزخانه، تولید |
| `نمره دهی بهداشت.xlsx` | ارزیابی بهداشتی | مسئول بهداشت | مترجم، مرجع، آشپزخانه، تولید |
| `نمره دهی تیم فروش.xlsx` | ارزیابی فروش/مهارت فروشندگی | مسئول فروش | مترجم، مرجع، کال‌سنتر، صندوق |
| `نمره دهی دوربین.xlsx` | ارزیابی از طریق مشاهده دوربین | مسئول دوربین/نظارت | مترجم، مرجع، سالن، کال‌سنتر، صندوق، آشپزخانه، تولید |
| `نمره دهی منابع انسانی.xlsx` | نظم ورود/خروج و ثبت انگشتی | مسئول منابع انسانی | مترجم، مرجع، سالن، کال‌سنتر، صندوق، آشپزخانه، تولید |
| `ارزیابی کل پرسنل.xlsx` | تجمیع نهایی همه فرم‌ها + Bonusها | ادمین/مدیریت | مترجم، مرجع، سالن، کال‌سنتر، صندوق، آشپزخانه، تولید، کانتر سالن(پاداش)، کانتر ارسال(پاداش)، سرپیک(پاداش)، سراپراتور(پاداش) |

## ۲. الگوی مشترک تمام فایل‌های «نمره دهی ...»

هر فایل دقیقاً همین ساختار را دارد:

- **Sheet «مترجم»**: دیکشنری دوستونی (انگلیسی → فارسی) برای تمام نام‌های فنی ستون‌ها و معیارها.
  این دقیقاً همان چیزی است که در Backend/Frontend برای Label فارسی استفاده شده (بدون تغییر نام
  فنی اصلی).
- **Sheet «مرجع»**: جدول‌های Lookup شامل تاریخ↔سال/ماه/هفته، لیست بخش‌ها، و فهرست پرسنل هر بخش
  (ستون‌های «اسامی پرسنل ...»).
- **Sheetهای بخش (سالن/کال‌سنتر/صندوق/آشپزخانه/تولید)**: هرکدام یک فرم ارزیابی واقعی با ستون‌های
  `Row, Year, Month, Week, Full name, [معیارها...], Score`. ستون `Score` همیشه یک فرمول Excel
  است که میانگین وزنی معیارها را محاسبه می‌کند.

## ۳. فرمول‌های استخراج‌شده (نمونه)

فرمول دقیق سلول `Score` برای هر Sheet با Regex از فرمول واقعی Excel استخراج و به
`backend/src/config/formsConfig.js` منتقل شد. نمونه‌ها:

```
[بازرس / آشپزخانه]  Score = (F*2 + G*2 + H + I + J + K + L + M) / 10
[بازرس / تولید]      Score = (F*2 + G*2 + H + I + J + K + L*3 + M) / 12
[بهداشت / آشپزخانه]  Score = (F*2 + G*2 + H + I + J*3 + K*4 + M*2 + L) / 16
[تیم فروش / کال‌سنتر] Score = (F*3 + G + H + I*2 + J*2 + K) / 10
[دوربین / سالن]      Score = (F*3 + G*2 + H*2 + I*2 + J) / 10
[منابع انسانی / همه] Score = (F*2 + G + H) / 4
[مسئول / سالن]       Score = (F*2 + G + H*2 + I*2 + J*5 + K*3 + L*3 + M*3 + N*3 + O*2) / 26
```

هر حرف ستون به یک معیار خاص با نام فنی و ترجمه فارسی (از Sheet «مترجم» همان فایل) نگاشت شده است.
جزئیات کامل معیارها/وزن‌ها/برچسب فارسی هر فرم-بخش در `backend/src/config/formsConfig.js` موجود
است.

## ۴. فایل «ارزیابی کل پرسنل» — تجمیع نهایی

این فایل دارای Sheetهای هم‌نام با بخش‌ها (سالن، کال‌سنتر، صندوق، آشپزخانه، تولید) است که در هرکدام
ستون‌هایی به نام‌های `responsible`, `System`, `camera`, `Human resources`, `Sales team`,
`hygiene`, `inspector` وجود دارد — این ستون‌ها **همان مقدار `Score` خروجی هر فرم مجزا** برای همان
پرسنل/دوره هستند (نه معیارهای خام). سپس یک فرمول وزن‌دار روی این ستون‌ها اعمال می‌شود:

| بخش | فرمول نهایی (Divisor = 5) |
|---|---|
| سالن | `(responsible×1.75 + System×1.75 + camera×1 + HR×0.5) / 5` |
| کال‌سنتر | `(responsible×1 + System×1.5 + camera×0.75 + HR×0.25 + SalesTeam×1.5) / 5` |
| صندوق | `(responsible×1.25 + System×1.5 + camera×0.5 + HR×0.25 + SalesTeam×1.5) / 5` |
| آشپزخانه | `(responsible×1.25 + System×1.25 + camera×0.75 + HR×0.25 + hygiene×0.75 + inspector×0.75) / 5` |
| تولید | همان فرمول آشپزخانه |

نگاشت نام ستون فایل تجمیعی → فایل منبع:

| ستون در ارزیابی کل | فایل منبع |
|---|---|
| `responsible` | نمره دهی مسئول (Score) |
| `camera` | نمره دهی دوربین (Score) |
| `Human resources` | نمره دهی منابع انسانی (Score) |
| `Sales team` | نمره دهی تیم فروش (Score) — فقط کال‌سنتر/صندوق |
| `hygiene` | نمره دهی بهداشت (Score) — فقط آشپزخانه/تولید |
| `inspector` | نمره دهی بازرس (Score) — فقط آشپزخانه/تولید |
| `System` | **در هیچ فایلی یافت نشد** — به ASSUMPTIONS در README مراجعه شود |

آیا Weight/ضریب وجود دارد؟ **بله** (جدول بالا). آیا فرمول خاصی وجود دارد؟ **بله**، دقیقاً همان که
در سلول Excel بود، بدون تغییر. آیا Bonus/Reward وجود دارد؟ **بله**، در ۴ Sheet جداگانه با منطق
کاملاً متفاوت (بخش ۶).

## ۵. فهرست پرسنل هر بخش (از Sheet «مرجع»)

| بخش | پرسنل |
|---|---|
| سالن | قاسمی، زنگنه، صفری، اسمائیلی، خیرخواه، سلامتی، فاضلی، سجادپور، حسن‌زاده، یعقوب‌نژاد، شهاب احمدی |
| کال‌سنتر (اپراتور) | جوان دوست، خوشدل، مظفری، باغ‌خوانی |
| صندوق | ترکانلو، غازی، دل‌بینا، فیضی |
| آشپزخانه | آشوری، محمدی، خوش‌خصلت، موذنی، سیدان، هاشم‌زهی، قربانی، مجیدی، رهنما، فاضل |
| تولید | جوان دوست، قاسمی، جهاندیده، رحیمی، رهنما، مسعودی، حسین‌زاده |

(نکته: برخی نام‌ها مثل «جوان دوست» و «رهنما» در بیش از یک بخش دیده شدند — در سامانه هر پرسنل به
ازای هر بخشی که در آن کار می‌کند یک رکورد مجزا در Sheet `Employees` دارد.)

## ۶. Sheetهای Bonus/Reward — منطق مجزا

این ۴ Sheet با فرم‌های ارزیابی معمولی تفاوت بنیادین دارند: به‌جای میانگین امتیاز ۱ تا ۵، یک
**مبلغ پایه روزانه (تومان)** دارند که به ازای هر تخلف مقدار مشخصی از آن کسر می‌شود:

| Sheet | مبلغ پایه | موارد کسر (هرکدام × مبلغ جریمه واحد) |
|---|---|---|
| کانتر سالن (پاداش) | ۲۰۰,۰۰۰ | تاخیر (۲۰,۰۰۰)، سردی غذا (۸۰,۰۰۰)، مغایرت نوشیدنی (۵۰,۰۰۰)، نارضایتی غذا (۲۰,۰۰۰) |
| کانتر ارسال (پاداش) | ۴۰۰,۰۰۰ | تاخیر (۲۰,۰۰۰)، سردی غذا (۳۰,۰۰۰)، مغایرت (۳۰,۰۰۰)، تایم بالای ۲۰دقیقه (۳,۰۰۰)، امتیاز ۱یا۲ اسنپ/تپسی (۱۵,۰۰۰) |
| سرپیک (پاداش) | ۲۰۰,۰۰۰ | فیش (۵,۰۰۰)، رفتار (۱۰۰,۰۰۰)، اشتباه (۲۰,۰۰۰)، تاخیر غذا (۲۰,۰۰۰)، سردی غذا (۳۰,۰۰۰) |
| سراپراتور (پاداش) | ۲۰۰,۰۰۰ | تماس ازدست‌رفته (۲,۰۰۰)، آدرس اشتباه (۲۰,۰۰۰)، لحن نامناسب (۳۰,۰۰۰)، تاخیر غذا (۳۰,۰۰۰)، سردی غذا (۵۰,۰۰۰) |

این مقادیر دقیقاً از فرمول واقعی هر سلول (مثلاً `=200000-H2-F2-D2-J2`) استخراج شده‌اند، نه از عدد
داخل عنوان ستون (که گاهی صرفاً یک آستانه توضیحی بود، نه ضریب واقعی).

## ۷. دیکشنری ترجمه (خلاصه از Sheet «مترجم»)

نمونه‌هایی از ۷۰ واژه استخراج‌شده (فهرست کامل در `backend/src/config/formsConfig.js` به‌صورت
`label` هر معیار پیاده شده است):

```
Full Name → نام و نام خانوادگی        Score → نمره
Discipline in entry and exit → نظم ورود و خروج
Fingerprint registration → ثبت صحیح انگشتی
Maintaining hygiene → رعایت بهداشت     Coverage → پوشش
Following the recipe → رعایت رسپی      Violations → تخلفات
Sales prowess → قدرت فروشندگی          Encounter → نوع برخورد
Health Card → کارت بهداشت              Critical signs → علائم بحرانی
responsible → مسئول                    System → سیستم
Camera → دوربین                        Human resources → منابع انسانی
```

## ۸. جمع‌بندی تصمیمات معماری برگرفته از این تحلیل

1. هر یک از ۶ فایل «نمره دهی ...» = یک **نقش ارزیاب** با فرم اختصاصی.
2. هر Sheet بخش داخل هر فایل = ترکیب مجاز (فرم × بخش)، دقیقاً همان که در Excel وجود داشت — هیچ
   ترکیب جدیدی اضافه نشد (مثلاً فرم «بازرس» فقط برای آشپزخانه/تولید فعال است، نه سالن).
3. فرمول محاسبه امتیاز هر فرم عیناً در `utils/scoring.js` پیاده‌سازی شد.
4. فرمول تجمیع نهایی («ارزیابی کل پرسنل») در همان فایل به‌صورت مجزا (`computeMasterScore`) پیاده
   شد و به Endpoint `GET /api/evaluations/master-score` متصل است.
5. تنها یک مورد مبهم (ستون System) طبق «قانون عدم حدس» در README تحت ASSUMPTIONS مستند شد.
